<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/bottomDialogListItem"
    style="@style/BottomDialogItem"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <android.support.v7.widget.AppCompatTextView
        android:id="@+id/textView"
        style="@style/BottomDialogItemLabel"
        android:textColor="@color/listItemText"
        tools:drawableStart="@drawable/ic_notifications_black_24dp"
        tools:text="@string/notify_when_have_to_leave" />

</FrameLayout>